import * as vscode from 'vscode';
import * as vstest from 'vscode-test';

suite('MI Parser', () => {
    test('Out of Band Record', () => {
        console.log("todo");
    });

    test('Result Record', () => {
        console.log("todo");
    });

    test('Async Record', () => {
        console.log("todo");
    });

    test('Stream Record', () => {
        console.log("todo");
    });
});